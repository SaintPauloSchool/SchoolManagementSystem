<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sms.system.mapper.SysDepartmentAdminMapper">

    <resultMap type="com.sms.system.entity.SysDepartmentAdmin" id="SysDepartmentAdminResult">
        <result property="id"           column="id"/>
        <result property="departmentId" column="department_id"/>
        <result property="userid"       column="userid"/>
        <result property="type"         column="type"/>
        <result property="subject"      column="subject"/>
        <result property="createTime"   column="create_time"/>
        <result property="updateTime"   column="update_time"/>
    </resultMap>

    <!-- 根据企业微信 userid 查询该用户的部门管理员记录 -->
    <select id="selectByUserid" resultMap="SysDepartmentAdminResult">
        SELECT id, department_id, userid, type, subject, create_time, update_time
        FROM sys_department_admin
        WHERE userid = #{userid}
    </select>

    <select id="selectByDepartmentIdAndUserid" resultMap="SysDepartmentAdminResult">
        SELECT id, department_id, userid, type, subject, create_time, update_time
        FROM sys_department_admin
        WHERE department_id = #{departmentId} AND userid = #{userid}
        LIMIT 1
    </select>

    <insert id="batchInsertDepartmentAdmins">
        INSERT INTO sys_department_admin (department_id, userid, type, subject, create_time, update_time)
        VALUES
        <foreach collection="admins" item="admin" separator=",">
            (#{admin.departmentId}, #{admin.userid}, #{admin.type}, #{admin.subject}, #{admin.createTime}, #{admin.updateTime})
        </foreach>
    </insert>

    <update id="updateByDepartmentIdAndUserid" parameterType="com.sms.system.entity.SysDepartmentAdmin">
        UPDATE sys_department_admin
        <set>
            <if test="type != null">type = #{type},</if>
            <if test="subject != null">subject = #{subject},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE department_id = #{departmentId} AND userid = #{userid}
    </update>

</mapper>
