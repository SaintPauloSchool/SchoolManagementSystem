<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sms.system.mapper.WecomSchoolDepartmentMemberMapper">

    <resultMap type="com.sms.system.entity.WecomSchoolDepartmentMember" id="WecomSchoolDepartmentMemberResult">
        <result property="id" column="id"/>
        <result property="userid" column="userid"/>
        <result property="name" column="name"/>
        <result property="departmentId" column="department_id"/>
        <result property="openUserid" column="open_userid"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!-- 批量查询多个部门的成员 -->
    <select id="selectMembersByDepartmentIds" resultMap="WecomSchoolDepartmentMemberResult">
        SELECT id, userid, name, department_id, open_userid, create_time, update_time
        FROM wecom_school_department_member
        WHERE department_id IN
        <foreach collection="departmentIds" item="deptId" open="(" separator="," close=")">
            #{deptId}
        </foreach>
        ORDER BY department_id ASC, name ASC
    </select>

    <!-- 根据 ID 批量查询部门成员 -->
    <select id="selectMembersByIds" resultMap="WecomSchoolDepartmentMemberResult">
        SELECT id, userid, name, department_id, open_userid, create_time, update_time
        FROM wecom_school_department_member
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY department_id ASC, name ASC
    </select>

    <!-- 根据企業微信 userid 字串查詢完整成員對象 -->
    <select id="selectByUserid" resultMap="WecomSchoolDepartmentMemberResult">
        SELECT id, userid, name, department_id, open_userid, create_time, update_time
        FROM wecom_school_department_member
        WHERE userid = #{userid}
        LIMIT 1
    </select>

    <insert id="batchInsertSchoolDepartmentMembers" parameterType="java.util.List">
        INSERT INTO wecom_school_department_member(userid, name, department_id, open_userid, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userid}, #{item.name}, #{item.departmentId}, #{item.openUserid}, #{item.createTime}, #{item.updateTime})
        </foreach>
        ON DUPLICATE KEY UPDATE
        name = VALUES(name),
        department_id = VALUES(department_id),
        open_userid = VALUES(open_userid),
        update_time = VALUES(update_time)
    </insert>

    <delete id="deleteMembersByDepartmentId" parameterType="Long">
        DELETE FROM wecom_school_department_member WHERE department_id = #{departmentId}
    </delete>

</mapper>
