<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sms.system.mapper.SysSchoolDepartmentMemberMapper">

    <resultMap type="com.sms.system.entity.SysSchoolDepartmentMember" id="SysSchoolDepartmentMemberResult">
        <result property="id" column="id"/>
        <result property="userid" column="userid"/>
        <result property="name" column="name"/>
        <result property="schoolDepartmentId" column="school_department_id"/>
        <result property="departmentId" column="department_id"/>
        <result property="openUserid" column="open_userid"/>
        <result property="type" column="type"/>
        <result property="studentId" column="student_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="memberColumns">
        id, userid, name, school_department_id, department_id, open_userid, type, student_id, create_time, update_time
    </sql>

    <select id="selectMembersByDepartmentIds" resultMap="SysSchoolDepartmentMemberResult">
        SELECT <include refid="memberColumns"/>
        FROM sys_school_department_member
        WHERE school_department_id IN
        <foreach collection="departmentIds" item="departmentId" open="(" separator="," close=")">
            #{departmentId}
        </foreach>
        ORDER BY school_department_id ASC, name ASC
    </select>

    <select id="selectMembersByIds" resultMap="SysSchoolDepartmentMemberResult">
        SELECT <include refid="memberColumns"/>
        FROM sys_school_department_member
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY school_department_id ASC, name ASC
    </select>

    <delete id="deleteMemberById" parameterType="Long">
        DELETE FROM sys_school_department_member
        WHERE id = #{id}
    </delete>

    <delete id="deleteByDepartmentId" parameterType="Long">
        DELETE FROM sys_school_department_member
        WHERE school_department_id = #{departmentId}
    </delete>

    <insert id="batchInsertMembers">
        INSERT INTO sys_school_department_member (userid, name, school_department_id, department_id, open_userid, type, student_id, create_time, update_time)
        VALUES
        <foreach collection="members" item="member" separator=",">
            (
                #{member.userid},
                #{member.name},
                #{member.schoolDepartmentId},
                #{member.departmentId},
                #{member.openUserid},
                #{member.type},
                #{member.studentId},
                #{member.createTime},
                #{member.updateTime}
            )
        </foreach>
    </insert>

    <select id="selectMembersByUserids" resultMap="SysSchoolDepartmentMemberResult">
        SELECT <include refid="memberColumns"/>
        FROM sys_school_department_member
        WHERE type = 2
          AND userid IN
        <foreach collection="userids" item="userid" open="(" separator="," close=")">
            #{userid}
        </foreach>
        ORDER BY school_department_id ASC, name ASC
    </select>

    <select id="selectHomeSchoolUseridsByUserids" resultType="java.lang.String">
        SELECT DISTINCT userid
        FROM sys_school_department_member
        WHERE type = 2
          AND userid IN
        <foreach collection="userids" item="userid" open="(" separator="," close=")">
            #{userid}
        </foreach>
    </select>

</mapper>
